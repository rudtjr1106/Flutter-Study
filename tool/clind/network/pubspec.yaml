name: tool_clind_network
description: tool clind network package.
publish_to: none

environment:
  sdk: 3.7.2
  flutter: 3.16.9

dependencies:
  flutter:
    sdk: flutter

  core_network:
    path: ../../../core/network

dev_dependencies:
